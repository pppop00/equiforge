# log-incident (Codex shell)

Draft a new `INCIDENTS.md` entry for the Anamnesis Research project. Human-in-the-loop: never append without explicit user confirmation.

The canonical procedure lives at `.agents/skills/log-incident/SKILL.md` — open and read that file in full, then follow it end-to-end. Do not paraphrase from memory; the body is the contract.

Argument convention: any text the user types after the slash-command invocation is the one-line description of what went wrong. If no argument was supplied, ask the user for it before proceeding.
